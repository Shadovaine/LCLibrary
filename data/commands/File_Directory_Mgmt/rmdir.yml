#--------------------------------
# Command YAML Template
#--------------------------------

# The name of the command (must match the actual Linux command)
name: rmdir

# Category this command belongs to (text_processing, file_management, user_group_mgmt, networking)
category: File_and_Directory_Management

# One-Line description of what the command does
description: Removes a directory if it is empty

# General syntax of the command
syntax: rmdir [OPTIONS]... DIRECTORY...

# Options: each flag (or set of flags) with an explanation
# Use " flags" as a list if there are multiple aliases (e.g. ["-i", "--ignore-case"])
# Optional: add "arg" if the optin expects a value
options:
  - flag: ["-ignore-fail-on-non-empty"]
    explains: Ignore each failure that is solely because a directory is non-empty
  - flag: ["-p", "--parents"] 
    explains: Remove DIRECTORY and its ancestors; e.g., 'rmdir -p a/b/c' is similar to 'rmdir a/b/c a/b a', but errors are ignored
  - flag: ["-v", "--verbose"]
    explains: Output a diagnostic for every directory processed
    
# Examples: List of example usages
examples:
  - rmdir -ignore-fail-on-non-empty project
  - rmdir -p project
  - rmdir -v project